version: '3.8'
services:
  subscription-app:
    build: .
    restart: always
    container_name: subscription-app
    ports:
      - "9074:3008"
    environment:
      - NODE_ENV=production
      - PORT=3008